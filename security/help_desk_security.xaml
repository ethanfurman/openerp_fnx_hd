!!! xml1.0

%openerp
    %data noupdate='1'

        %record model='ir.module.category' #fnx_help_desk
            @name: Help Desk
            @sequence: 90
            @description: Report and solve various technical problems.

        %record model='res.groups' #fnx_help_desk_user
            @name: User
            @category_id ref='fnx_help_desk'
            @comment: User can edit solutions

        %record model='res.groups' #fnx_help_desk_manager
            @name: Manager
            @category_id ref='fnx_help_desk'
            @implied_ids eval="[(4, ref('fnx_help_desk_user'))]"
            @comment: Manager can set EvS status
